#include <bits/stdc++.h>

using namespace std;

int main() {
	int n; cin>>n;

	vector<int> v(n);
	for (int i=0;i<n;i++) {
		cin>>v[i];
	}

	// brute force / complete search, try __gcd(i,j) every combo

	// int divisor[1000000]. increment the divisor 
	// when you break apart the numbers. 

	
	

	
	return 0;
}